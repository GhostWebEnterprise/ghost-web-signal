name: Super-Linter

on:
  push:
    branches:
      - GWSignal.main
      - main
  pull_request:
    branches:
      - GWSignal.main
      - main
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

concurrency:
  group: super-linter-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  super-linter:
    name: Super-Linter
    runs-on: ubuntu-latest
    # Incremental linting (only changed files) finishes in minutes; full
    # sweeps of the Signal/Molly codebase exceeded 60-minute job limits.
    timeout-minutes: 60
    permissions:
      contents: read
      statuses: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Run Super-Linter
        uses: super-linter/super-linter@4ce20838b8ab83717e78138c5b3a1407148e0918 # v8.7.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEFAULT_BRANCH: GWSignal.main
          VALIDATE_ALL_CODEBASE: false
          VALIDATE_KOTLIN: true
          VALIDATE_JAVA: true
          VALIDATE_GROOVY: true
          VALIDATE_YAML: true
          VALIDATE_JSON: true
          VALIDATE_MARKDOWN: true
          VALIDATE_GITHUB_ACTIONS: true
          VALIDATE_EDITORCONFIG: true
          VALIDATE_GIT_MERGE_CONFLICT_MARKERS: true
          IGNORE_GITIGNORED_FILES: true
          FILTER_REGEX_EXCLUDE: '(^|/)(build|\.gradle|\.idea|generated|node_modules|reproducible-builds)(/|$)|(^|/)(local\.properties)$'
          ENABLE_GITHUB_ACTIONS_STEP_SUMMARY: true
          SAVE_SUPER_LINTER_SUMMARY: true
